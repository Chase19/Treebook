<%= form_for(@ststu) do |f| %>
  <% if @ststu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ststu.errors.count, "error") %> prohibited this ststu from being saved:</h2>

      <ul>
      <% @ststu.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
